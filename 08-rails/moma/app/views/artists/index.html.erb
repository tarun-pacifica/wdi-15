<h1>Artists</h1>

<% @artists.each do |artist| %>
  <%= image_tag artist.image, :class => 'thumb' %>
  <ul>
    <li><%= artist.name %></li>
    <li><%= artist.nationality %></li>
    <li><%= artist.dob.strftime '%A %B %e, %Y' %></li>
    <li><%= artist.period %></li>
  </ul>
<% end %>
