<p><%= pluralize(@friend_count1, 'person') %> online.</p>
<p><%= pluralize(@friend_count2, 'person') %> online.</p>

<p>
  <%= truncate(@story, :length => 20) %>
</p>

<ul>
  <% @numbers.each do |n| %>
    <li style="background-color: <%= cycle('red', 'green', 'orange', 'purple') %>"><%= n %></li>
  <% end %>
</ul>
